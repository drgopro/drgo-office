<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    private const TITLE = '업데이트 노트 8/21 (금)';

    /**
     * 2026-08-21 배포분(8/19 노트 발행 이후) 업데이트 노트를 위키 '업데이트' 게시판에 등록.
     * 운영 DB에 직접 접근할 수 없어 배포 자동 마이그레이션으로 발행한다.
     * 사용자가 없는 환경(테스트 sqlite)에서는 아무것도 하지 않아 기존 테스트에 영향 없음.
     */
    public function up(): void
    {
        $authorId = DB::table('users')->where('role', 'master')->orderBy('id')->value('id')
            ?? DB::table('users')->orderBy('id')->value('id');
        if (! $authorId) {
            return; // 테스트/빈 DB — 스킵
        }
        if (DB::table('wikis')->where('type', 'update')->where('title', self::TITLE)->exists()) {
            return; // 재실행 안전
        }

        DB::table('wikis')->insert([
            'title' => self::TITLE,
            'type' => 'update',
            'category' => '업데이트',
            'category_id' => null,
            'content' => $this->content(),
            'is_pinned' => 0,
            'is_draft' => 0,
            'created_by' => $authorId,
            'updated_by' => $authorId,
            'created_at' => now(),
            'updated_at' => now(),
        ]);
    }

    public function down(): void
    {
        DB::table('wikis')->where('type', 'update')->where('title', self::TITLE)->delete();
    }

    private function content(): string
    {
        return <<<'HTML'
<h2>8/21 (금)</h2>
<h3>페이앱 결제현황 — 페이앱 자체 결제까지 전부 한 화면에</h3>
<ul>
<li><strong>페이앱에서 직접 만든 결제도 표시</strong>: 견적서로 보낸 결제요청뿐 아니라 페이앱 판매자 페이지에서 직접 만든 결제 링크의 결제·취소 내역도 자동으로 수집해 함께 보여줍니다. '페이앱 자체' 뱃지와 상품명이 붙고, 매출전표 버튼으로 바로 이동합니다. 홈페이지(카페24) 연동은 그대로 유지됩니다.</li>
<li><strong>결제 취소를 오피스에서 바로</strong>: 결제 대기 건은 '요청 취소'로 결제 링크를 철회하고, 결제완료 건은 '결제 취소'로 승인취소합니다. 이미 정산이 끝난 결제는 자동으로 환불 요청으로 접수되고, 페이앱에서 처리되면 상태가 갱신됩니다. 취소 권한은 관리 페이지의 팀 관리(입금 내역 &gt; 결제 취소)에서 팀별로 부여합니다.</li>
<li><strong>결제 ID 표시와 중복 방지</strong>: 각 건에 페이앱 결제요청 번호(ID)가 표시돼 같은 결제인지 바로 확인할 수 있고, 통보가 뒤늦게 재전송돼도 실제 요청/결제 시각으로 기록해 같은 결제가 두 줄로 보이거나 취소된 결제가 결제완료로 되돌아가는 일을 막습니다.</li>
<li><strong>엑셀 가져오기</strong>: 페이앱 판매자 페이지에서 내려받은 결제내역 엑셀을 업로드하면 과거 내역(취소건 포함)이 백필됩니다. 8월분처럼 웹훅 연동 전의 결제도 채울 수 있습니다.</li>
</ul>
<h3>의뢰자 — 관계자와 주소를 더 풍부하게</h3>
<ul>
<li><strong>관계자(매니저) 관리</strong>: 의뢰자마다 매니저·소속사 담당 등 관계자를 최대 10명까지 등록합니다 (이름·연락처·관계·메모). 의뢰자 검색에서 관계자 이름이나 연락처로 검색해도 해당 의뢰자가 나옵니다. 연락처는 입력 시 하이픈이 자동으로 붙습니다.</li>
<li><strong>주소 최대 4개</strong>: 주소 1이 메인이고 필요하면 주소 4까지 추가할 수 있습니다. 캘린더에서 의뢰자 주소를 일정에 넣을 때 주소가 여러 개면 어떤 주소를 쓸지 고르는 팝업이 뜹니다.</li>
</ul>
<h3>재고관리 — 대량 수정과 필터</h3>
<ul>
<li><strong>전체 편집</strong>: 버튼 한 번으로 목록 전체가 입력 모드로 바뀌어 제품명·매입가·판매가·안전재고·현재고를 한 번에 고치고 일괄 저장합니다. 수정하고 싶은 칸을 클릭하면 바로 입력이 시작됩니다.</li>
<li><strong>재고 수량 필터</strong>: 재고 0개만, 부족(안전재고 미만), 특정 수량 이상/이하로 걸러 봅니다. 세트 상품은 구성품으로 계산한 조립 가능 수 기준입니다.</li>
<li><strong>적정 판매가 안내</strong>: 매입가만 넣고 판매가가 비어 있으면 마진율 10% 기준의 적정 판매가가 입력칸 안에 안내로 표시됩니다.</li>
<li>PC 기준 페이지 가로폭을 넓혀 편집 시 버튼이 잘리지 않습니다.</li>
</ul>
<h3>프로젝트 — 익명 의뢰자 정리</h3>
<ul>
<li>익명(의뢰자 미연동) 프로젝트에서 상담 이력을 등록하면 서버 오류가 나던 문제를 수정했습니다.</li>
<li>익명 프로젝트는 상세 화면이 프로젝트명과 상담 이력 중심으로 간소화되고, 새 프로젝트 모달도 익명 체크 시 유형까지만 남습니다.</li>
<li>통계에서 익명 프로젝트는 상담 건수에는 포함되지만 진행 중 파이프라인에는 잡히지 않습니다.</li>
</ul>
<h3>캘린더 — 주소 조회에 네이버 지도 추가</h3>
<ul>
<li>주소 조회 버튼이 카카오 맵·동선, 네이버 맵·동선 네 가지로 늘었습니다. 동선 조회는 출발지가 사업장(서울 동작구 장승배기로 142)으로 고정되고 도착지에 일정의 주소가 채워진 상태로 열립니다.</li>
</ul>
<h3>위키 · 통계 — 열람 권한</h3>
<ul>
<li>위키 게시물마다 열람 가능한 팀을 제한할 수 있습니다. 제한된 글은 목록·검색·대시보드에서도 해당 팀에게만 보입니다.</li>
<li>수정 권한이 없는 사용자에게는 수정 버튼이 아예 표시되지 않습니다.</li>
<li>통계 페이지도 팀 권한(통계)으로 열람을 관리합니다.</li>
</ul>
<h3>배송</h3>
<ul>
<li>배송완료 후 15일이 지난 송장은 실시간 위치 추적을 중단합니다 — 조회 횟수를 아끼고, 완료되지 않은 송장은 계속 추적합니다.</li>
</ul>
HTML;
    }
};
