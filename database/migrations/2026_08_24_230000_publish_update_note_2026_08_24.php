<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    private const TITLE = '업데이트 노트 8/24 (월)';

    /**
     * 2026-08-24 배포분(8/21 노트 발행 이후) 업데이트 노트를 위키 '업데이트' 게시판에 등록.
     * 운영 DB에 직접 접근할 수 없어 배포 자동 마이그레이션으로 발행한다.
     * 사용자가 없는 환경(테스트 sqlite)에서는 아무것도 하지 않아 기존 테스트에 영향 없음.
     */
    public function up(): void
    {
        $authorId = DB::table('users')->where('role', 'master')->orderBy('id')->value('id')
            ?? DB::table('users')->orderBy('id')->value('id');
        if (! $authorId) {
            return; // 테스트/빈 DB — 스킵
        }
        if (DB::table('wikis')->where('type', 'update')->where('title', self::TITLE)->exists()) {
            return; // 재실행 안전
        }

        DB::table('wikis')->insert([
            'title' => self::TITLE,
            'type' => 'update',
            'category' => '업데이트',
            'category_id' => null,
            'content' => $this->content(),
            'is_pinned' => 0,
            'is_draft' => 0,
            'created_by' => $authorId,
            'updated_by' => $authorId,
            'created_at' => now(),
            'updated_at' => now(),
        ]);
    }

    public function down(): void
    {
        DB::table('wikis')->where('type', 'update')->where('title', self::TITLE)->delete();
    }

    private function content(): string
    {
        return <<<'HTML'
<h2>8/24 (월)</h2>
<h3>견적서 출력물 — 디자인 전면 리뉴얼</h3>
<ul>
<li><strong>새 디자인</strong>: 네이비/슬레이트 톤으로 전면 개편했습니다. 상단 네이비 밴드(견적서 제목 + 작성일·발행일시·유효기간), 주문 정보·판매처 카드, 대분류 슬레이트 밴드와 분류별 소계, 하단 네이비 합계 밴드로 구성됩니다.</li>
<li><strong>직인 표시</strong>: 관리 페이지의 판매처 설정에서 직인 이미지를 등록하면(배경 투명 PNG 권장) 견적서 판매처 영역의 상호명에 살짝 겹치는 위치에 도장이 표시됩니다. 내부 인쇄·이미지 저장·의뢰자 공개 링크 모두 동일하게 나옵니다.</li>
<li><strong>가로 폭 확대</strong>: 출력물 폭을 넓혀 긴 제품명(부품 구성 등)이 두 줄로 꺾이는 일이 크게 줄었습니다. 이미지/PDF 저장 폭도 함께 맞췄습니다.</li>
<li><strong>모바일 반응형</strong>: 의뢰자가 휴대폰으로 공개 링크를 열면 번호·소요시간 열이 접히고 정보 카드가 세로로 쌓여 가로 스크롤 없이 읽을 수 있습니다.</li>
<li>소요시간이 전부 비어 있는 견적서는 소요시간 열이 자동으로 숨겨집니다.</li>
</ul>
<h3>견적서 작성 화면 — 같은 테마로 리뉴얼 + 카테고리 필터 강화</h3>
<ul>
<li><strong>디자인 통일</strong>: 작성 화면과 프리셋 편집 창도 출력물과 같은 네이비/슬레이트 테마로 바뀌었고, 전체 글자 크기를 한 단계 키웠습니다.</li>
<li><strong>카테고리 드릴다운</strong>: 제품 리스트에서 1차 카테고리 칩을 누르면 하위 카테고리 칩이 계단식으로 열려 2·3차까지 좁혀서 필터할 수 있습니다. 제품마다 SKU 옆에 카테고리 전체 경로(예: 비디오 › 카메라 › R 시리즈)도 표시됩니다.</li>
<li><strong>분류 소계</strong>: 각 대분류 블록 맨 아래에 소계 밴드가 표시됩니다.</li>
<li>저장이 완료되면 저장되었다는 알림이 뜹니다.</li>
</ul>
<h3>주문/배송 — 견적서에서 발주·배송까지 관리</h3>
<ul>
<li><strong>주문/배송 모드</strong>: 견적서 상단의 '주문/배송' 버튼으로 화면을 전환합니다. 전환 중에는 견적서 번호 옆에 '주문/배송 페이지' 라벨이 붙고 수량·판매가·합계가 수정 잠금됩니다.</li>
<li><strong>주문완료 표시</strong>: 항목마다 '주문완료' 버튼을 누르면 제품명에 옅은 녹색 표시가 붙고 즉시 저장됩니다. '취소' 버튼으로 해제합니다.</li>
<li><strong>배송 정보(운송장)</strong>: '배송 정보' 버튼을 누르면 새 창에서 택배사와 운송장 번호를 여러 개 한 번에 등록할 수 있습니다. 캘린더 배송 추적과 같은 방식으로 등록 즉시 조회되고, 배송완료/배송 중 상태와 마지막 위치가 표시되며 매일 자동 갱신됩니다.</li>
<li><strong>제품 메모</strong>: 주문/배송 모드에서는 제품 관리에 적어둔 제품 메모가 제품명 아래 회색으로 표시됩니다. 직원 화면 전용으로, 의뢰자 견적서와 출력물에는 나오지 않습니다.</li>
<li>배송완료 후 2일이 지난 송장은 추적을 중단합니다 (기존 15일에서 단축 — 조회 횟수 절약).</li>
</ul>
<h3>제품 관리 — 소요시간 설정</h3>
<ul>
<li>제품마다 '소요시간 사용'을 체크하고 기본 소요시간(예: 2시간)을 넣을 수 있습니다. 체크한 제품만 견적서에서 소요시간 입력칸이 표시되고, 담을 때 기본값이 미리 채워집니다. 소요시간이 필요 없는 제품은 입력칸 자체가 나오지 않습니다.</li>
</ul>
<h3>견적서 목록 — 편의 개선</h3>
<ul>
<li><strong>링크 복사 버튼</strong>: 목록에서 바로 의뢰자용 견적서 링크를 복사합니다.</li>
<li>출력 드롭다운 메뉴가 목록 카드에 잘리던 문제와, 중간 해상도에서 금액·버튼·날짜가 두 줄로 깨지던 문제를 수정했습니다. 화면이 좁으면 목록이 가로 스크롤됩니다.</li>
</ul>
<h3>데이터 검증 강화</h3>
<ul>
<li>견적서 품목의 필수값(제품명·판매가·수량·합계)을 저장 단계에서 검증해, 깨진 데이터가 저장돼 출력물에서 오류가 나는 경로를 차단했습니다.</li>
<li>견적서의 프로젝트 연동은 해당 의뢰자 소유의 프로젝트만 가능합니다.</li>
</ul>
<h3>보안 강화</h3>
<ul>
<li><strong>검색엔진 비색인</strong>: 오피스가 구글 등 검색에 노출되지 않도록 크롤링을 전면 차단하고 모든 페이지에 비색인 헤더를 이중으로 적용했습니다. 의뢰자 공개 견적서도 검색에 잡히지 않습니다.</li>
<li><strong>링크 유출 방지</strong>: 공개 견적서에서 외부 링크를 눌러도 견적서 주소(토큰)가 상대 사이트에 전달되지 않습니다.</li>
<li><strong>로그인 보호</strong>: 같은 계정으로 1분에 5회 로그인에 실패하면 잠시 잠깁니다. 클릭재킹·MIME 스니핑 방지 등 표준 보안 헤더도 함께 적용했습니다.</li>
</ul>
HTML;
    }
};
